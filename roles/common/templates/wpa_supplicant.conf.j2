# {{ ansible_managed }}

country=US
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1

{% for wifi in wifis %}
network={
	ssid="{{ wifi.ssid }}"
	psk="{{ wifi.psk }}"
	key_mgmt={{ wifi.keymgmt }}
}
{% endfor %}
